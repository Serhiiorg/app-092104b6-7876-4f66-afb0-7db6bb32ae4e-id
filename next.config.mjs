/** @type {import('next').NextConfig} */
    const nextConfig = {
      images: {
        domains: ['images.unsplash.com', 'fonts.googleapis.com', 'www.youtube.com'],
      },
    };
    
    export default nextConfig;